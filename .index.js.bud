/**
 * Bud file for index.js
 */

"use strict";


module.exports = {
    force: true,
    mode: '444',
    data: {
        END_BRACE: '}',
        pkg: require('./package.json'),
        links: require('./docs/readme/.links.json')
    }
};

if (!module.parent) {
    require('coz').render(__filename);
}