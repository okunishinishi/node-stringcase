/**
 * Test case for dotcase.
 * Runs with mocha.
 */
'use strict'

const dotcase = require('../lib/dotcase.js')
const assert = require('assert')

describe('dotcase', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Dotcase', async () => {

  })
})

/* global describe, before, after, it */
